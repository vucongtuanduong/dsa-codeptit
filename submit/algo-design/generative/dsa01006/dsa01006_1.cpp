#include <bits/stdc++.h>
using namespace std;
vector<int> a(n + 1);
vector<bool> used(n + 1, false);
vector<vector<int>> res;
void testCase();
void Try(int i);
int main() {
    // Write your code here
    int t;
    cin >> t;
    while (t--) {
        testCase();
        cout << endl;
    }
    return 0;
}
void Try(int i) {
    for (int j = 1; j < a.size(); j++) {
        
    }
}
void testCase() {
    int n;
    cin >> n;
    Try(1);
    
}