#include <bits/stdc++.h>
using namespace std;
int n, k, s;
void testCase();
int main() {
    // Write your code here
    while (true) {
        cin >> n >> k >> s;
        if (n == 0 && k == 0 && s == 0) {
            return;
        }
        testCase();

    }
    return 0;
}
void testCase() {
    
    
}