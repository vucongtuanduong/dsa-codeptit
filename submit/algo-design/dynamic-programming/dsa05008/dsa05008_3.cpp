#include <bits/stdc++.h>
using namespace std;
void testCase() {
    int n, s;
    cin >> n >> s;
    int a[n];
    for (int i = 0; i < n; i++){ 
        cin >> a[i];
    }
}
int main() {
    // Write your code here
    int t;
    cin >> t;
    while (t--) {
        testCase();
        cout << endl;
    }
    return 0;
}
