 #include <bits/stdc++.h>
using namespace std;

int main() {
    // Write your code here
    int n;
    cin >> n;
    int ans = INT_MAX;
    if (n % 2 == 0) {
        ans = min(ans, n / 2);

    }
    if (n % 3)
    vector<int> dp(n, 0);
    return 0;
}
